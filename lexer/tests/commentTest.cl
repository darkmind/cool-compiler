(* This is a good comment *)
(* This is a comment that spans many lines
like here
and here
and here *)
*)
-- inline comment
identifierHere -- a comment after some stuff
(* block comment here --inline comment within a block comment*) --inline comment after a block comment *)*)*) some unmatched parens
(* This comment goes past eof
